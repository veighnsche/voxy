#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || true)"
if [[ -z "${repo_root}" ]]; then
  echo "[pre-commit] Could not locate repository root." >&2
  exit 1
fi

exec "${repo_root}/scripts/dev/pre-commit-checks.sh"
